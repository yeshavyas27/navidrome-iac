# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome-app
  namespace: navidrome-canary

spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: navidrome-app
  template:
    metadata:
      labels:
        app: navidrome-app
    spec:
      containers:
        - name: navidrome-app
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: navidrome-app
  namespace: navidrome-canary
spec:
  selector:
    app: navidrome-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  externalIPs:
    - {{ .Values.service.externalIP }}
---
# ReferenceGrant allows HTTPRoute in navidrome-platform to reference this Service
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-platform-httproute
  namespace: navidrome-canary
spec:
  from:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    namespace: navidrome-platform
  to:
  - group: ""
    kind: Service

